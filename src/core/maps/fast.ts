import type { MapDefinition } from './types.ts';
import { FAST_MAPS_AUTHORED } from './authored.ts';

/** The 5 Fast-play maps (small, fewer locations/links, shorter decks). */
export const FAST_MAPS: MapDefinition[] = FAST_MAPS_AUTHORED;
